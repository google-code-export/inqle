<!-- ===================================================================== -->
<!-- Custom targets called from a project's generated build.xml            -->
<!-- Set customBuildCallbacks=<path/to/this/file> in your build.properties.-->
<!-- ===================================================================== -->
<project name="Build specific targets and properties" default="noDefault">

	<!-- ===================================================================== -->
	<!-- Default target                                                        -->
	<!-- ===================================================================== -->
	<target name="noDefault">
		<echo message="This file must be called with explicit targets" />
	</target>
	
	<!-- ===================================================================== -->
	<!-- Steps to do before the target gather.bin.parts                        -->
	<!-- Available parameters :                                                -->
	<!--  destination.temp.folder - the directory plugins will be collected to -->
	<!--  feature.directory - the directory containing the resulting feature   -->
	<!-- ===================================================================== -->
	<!--  This deletes the directory to which we are deploying -->
	<target name="pre.gather.bin.parts">
		<delete includeemptydirs="true">
	    <fileset dir="${feature.directory}/../../" includes="**/*"/>
	  </delete>
	</target>
	
	<!-- ===================================================================== -->
	<!-- Steps to do after the target gather.bin.parts                         -->
	<!-- Available parameters :                                                -->
	<!--  destination.temp.folder - the directory plugins will be collected to -->
	<!--  feature.directory - the directory containing the resulting feature   -->
	<!-- ===================================================================== -->
	<!--  This copies the contents of the folder dist_root to the directory to which we are deploying -->
	<target name="post.gather.bin.parts">
		<copy todir="${feature.directory}/../../" includeemptydirs="true">
			<fileset dir="dist_root">
			</fileset>
		</copy>
	</target>

</project>
