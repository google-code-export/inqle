// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package org.inqle.domain;

import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Date;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.inqle.domain.Account;
import org.inqle.domain.Concept;
import org.inqle.domain.ConceptDataOnDemand;
import org.inqle.domain.ConceptTranslation;
import org.inqle.domain.ConceptTranslationDataOnDemand;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ConceptTranslationDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ConceptTranslationDataOnDemand: @Component;
    
    private Random ConceptTranslationDataOnDemand.rnd = new SecureRandom();
    
    private List<ConceptTranslation> ConceptTranslationDataOnDemand.data;
    
    @Autowired
    ConceptDataOnDemand ConceptTranslationDataOnDemand.conceptDataOnDemand;
    
    public ConceptTranslation ConceptTranslationDataOnDemand.getNewTransientConceptTranslation(int index) {
        ConceptTranslation obj = new ConceptTranslation();
        setConcept(obj, index);
        setConceptDescription(obj, index);
        setConceptName(obj, index);
        setCreated(obj, index);
        setCreatedBy(obj, index);
        setLang(obj, index);
        setUpdated(obj, index);
        setUpdatedBy(obj, index);
        return obj;
    }
    
    public void ConceptTranslationDataOnDemand.setConcept(ConceptTranslation obj, int index) {
        Concept concept = conceptDataOnDemand.getRandomConcept();
        obj.setConcept(concept);
    }
    
    public void ConceptTranslationDataOnDemand.setConceptDescription(ConceptTranslation obj, int index) {
        String conceptDescription = "conceptDescription_" + index;
        obj.setConceptDescription(conceptDescription);
    }
    
    public void ConceptTranslationDataOnDemand.setConceptName(ConceptTranslation obj, int index) {
        String conceptName = "conceptName_" + index;
        obj.setConceptName(conceptName);
    }
    
    public void ConceptTranslationDataOnDemand.setCreated(ConceptTranslation obj, int index) {
        Date created = new Date(new Date().getTime() + 10000000L);
        obj.setCreated(created);
    }
    
    public void ConceptTranslationDataOnDemand.setCreatedBy(ConceptTranslation obj, int index) {
        Account createdBy = null;
        obj.setCreatedBy(createdBy);
    }
    
    public void ConceptTranslationDataOnDemand.setLang(ConceptTranslation obj, int index) {
        String lang = "lang_" + index;
        obj.setLang(lang);
    }
    
    public void ConceptTranslationDataOnDemand.setUpdated(ConceptTranslation obj, int index) {
        Date updated = new Date(new Date().getTime() + 10000000L);
        obj.setUpdated(updated);
    }
    
    public void ConceptTranslationDataOnDemand.setUpdatedBy(ConceptTranslation obj, int index) {
        Account updatedBy = null;
        obj.setUpdatedBy(updatedBy);
    }
    
    public ConceptTranslation ConceptTranslationDataOnDemand.getSpecificConceptTranslation(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ConceptTranslation obj = data.get(index);
        Long id = obj.getId();
        return ConceptTranslation.findConceptTranslation(id);
    }
    
    public ConceptTranslation ConceptTranslationDataOnDemand.getRandomConceptTranslation() {
        init();
        ConceptTranslation obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ConceptTranslation.findConceptTranslation(id);
    }
    
    public boolean ConceptTranslationDataOnDemand.modifyConceptTranslation(ConceptTranslation obj) {
        return false;
    }
    
    public void ConceptTranslationDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ConceptTranslation.findConceptTranslationEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ConceptTranslation' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ConceptTranslation>();
        for (int i = 0; i < 10; i++) {
            ConceptTranslation obj = getNewTransientConceptTranslation(i);
            try {
                obj.persist();
            } catch (ConstraintViolationException e) {
                StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getConstraintDescriptor()).append(":").append(cv.getMessage()).append("=").append(cv.getInvalidValue()).append("]");
                }
                throw new RuntimeException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
